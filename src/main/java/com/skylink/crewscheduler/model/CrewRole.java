package com.skylink.crewscheduler.model;

/**
 * The job function a crew member performs. Pilots and flight attendants are
 * scheduled the same way operationally, but carry different qualifications.
 */
public enum CrewRole {
    PILOT,
    FLIGHT_ATTENDANT
}
